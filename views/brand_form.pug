extends layout 

block content 
    .brandFormContainer
        h1=title

        form(method="POST" action="" class="brandCreateForm")
            .brandFormGroup
                label(for="brandName") Brand Name:
                input#brandName.form-control(type="text" name="name" required="true" value=(undefined===brand? "" : brand.name)) 
            button(class="createBtn" type="submit") Submit

        if errors 
            ul 
                for error in errors 
                    li!=error.msg